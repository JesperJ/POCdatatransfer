# Traefik static configuration
# Docs: https://doc.traefik.io/traefik/

api:
  dashboard: false

entryPoints:
  web:
    address: ":80"
    http:
      redirections:
        entryPoint:
          to: websecure
          scheme: https
  websecure:
    address: ":443"
    transport:
      lifeCycle:
        graceTimeOut: 10s   # drain in-flight requests before shutdown

providers:
  docker:
    exposedByDefault: false   # services must opt in via traefik.enable=true label
    network: traefik-public
  file:
    directory: /etc/traefik/dynamic
    watch: true              # hot-reload dynamic config without restarting Traefik

# No certificatesResolvers — Traefik uses its built-in self-signed cert
